var router = require( "express" ).Router()
var db = require( "db" )()

router.get( "/", ( req, res ) => {
	 
})

router.post( "/", ( req, res ) => {
	r = req.body
	route = r.route
	date = r.date

		
})

module.exports = router